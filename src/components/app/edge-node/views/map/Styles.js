import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles((theme) => ({
  root: {
    height: '100vh',
    width: '100%',
  },
  card: {
    minWidth: 100,
  },
}));

export default styles;
