import { makeStyles } from '@material-ui/styles';

const styles = makeStyles(theme => ({
  root: {
    paddingTop: 64,
    height: '100%',
  },
  content: {
    height: '100%',
  },
}));

export default styles;
